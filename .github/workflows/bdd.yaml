name: BDD tests


on:
  push:
    # branches: ["main"]
  pull_request:

jobs:
  bdd:
    name: BDD tests
    uses: joselsegura/processing-tools/.github/workflows/bdd.yaml@bdd-notification-service
    with:
      service: parquet-factory
      use_kafka: true
      use_minio: true
      use_pushgateway: true
